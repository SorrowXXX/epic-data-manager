# EPIQA Data Manager

## Tech Stack
- React + Vite (Frontend)
- Express + MariaDB (Backend)
- Docker + Docker Compose

## Setup
```bash
docker-compose up --build
```

Edit the `.env` files with your DB credentials.
